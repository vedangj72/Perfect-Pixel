<!-- res/drawable/custom_toast_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#CC333333"/> <!-- Background color (semi-transparent gray) -->
    <corners android:radius="12dp"/>   <!-- Rounded corners -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>       <!-- Padding inside the toast -->
</shape>
